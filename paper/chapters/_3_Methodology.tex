\chapter{Methodology}

\section{Experimental Design}

To investigate the influence of the construction strategy on the quality of the BVH, an experiment is implemented. 
Three algorithms are to be implemented, those being: top-down, bottom-up, and incremental, following the classification presented by Ericson\cite{ericson2004real}
The experiment will focus on static two-dimensional collision detection, therefore particles will not move after dataset generation, and each hierarchy is generated completly before traversal is done.

The independet variable of the experiment will be the BVH construction strategy, the dependent variables are the number of bounding volume and primitive collision checks performed during traversal.
A bounding volume collision check is defined as a collision check where one of the aabb in the check contains more than one primitive particle.

To isolate the effect of the construction strategy, all other components of the collision detecion procedure are kept constant between experiment runs.
All strategies receive the same datasets and use the same geometric representation, collision detection and traversal procedure. 
This is meant to assure, that differences in the recorded number of checks can primarly be attributed to the differences in the structure generated by the respective construction algorithms.

\section{BVH construction}

The general principles of the three construction mechanisms are explained in the litereature review section.
This section therefore will focus on the specific implementations of the algorithms for this experiment.


\section{Traversal and Evaluation}

After construction, each hierarchy is queried using the same self-traversal procedure. 
A pair of subtrees is discarded when their bounding volume do not overlap. 
If the bounding volume does overlap, traversal continues with the remaining child-node combination. A primitive test is only performed when a subtreee conssits of two direct leaf-type children.

Every evaluation records the number of necessary AABB overlap tests, and the number of primitive collision tests seperatly.
Keeping the measures seperatly was chosen, as they represent two different operations that potentially come with a different computational cost, which therefore is relevant for measuring tree-quality.

% Tree height and the total area of bounding volumes stored in the hierarchy is collected as additional/supplementary metrics.
% They may help explain differences in behaviour, but are not considered a primary measurement as this is not the goal of this work.
%
% Mal gucken ob ich das so reinnehme, ich adde es erstmal als comment